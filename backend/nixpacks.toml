[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "python -m pip install --user yt-dlp"
]

[start]
cmd = "export PATH=$PATH:/root/.local/bin && node src/server.js"